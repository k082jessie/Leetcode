class Solution:
    def flipAndInvertImage(self, image: List[List[int]]) -> List[List[int]]:
        ans = []
        for row in image:
            ans.append([1-val for val in row[::-1]])
        return ans
